# הנחיות פיתוח — roh-system

מערכת ניהול לקוחות ויתרות למשרד רו"ח. האפיון המלא והמחייב: `docs/tech-spec.md`.

## כללי ברזל (מהאפיון — אין לעבור עליהם)

1. המערכת לעולם לא יוצרת, לא מספררת ולא מאחסנת מסמך מס כמקור — רק הספק (פייפרלס/Sumit).
2. אין מחיקה ואין עריכה של מסמך שהונפק. ביטול = מסמך זיכוי.
3. אישור אנושי לפני כל הנפקה.
4. אין שליחה חוזרת אוטומטית לספק — קודם אימות דרך חיפוש מסמכים.
5. היתרה תמיד מחושבת (view `client_balances`) — לעולם לא נשמרת כשדה.
6. מניעת כפילות בשתי שכבות: `row_hash` על תנועות בנק, `idempotency_key` על מסמכים.
7. כל קריאה לספק עוברת דרך המתאם (`DocumentProvider`) — אין קריאות ישירות בלוגיקה.
8. כל פעולה מהותית נרשמת ב-`audit_log` (דרך `writeAudit`).
9. פיתוח ובדיקות על נתונים ממוסכים בלבד — אין נתוני אמת לפני חתימת ההסכם.
10. סכומים כספיים לעולם לא עוברים דרך float — numeric ב-DB, מחרוזות בקוד.

## קונבנציות

- ממשק בעברית, RTL מלא. `color-scheme: light` קבוע — אין מצב כהה.
- גישה ל-DB: ספריית `postgres` עם `transform: postgres.camel` — עמודות snake_case
  ב-SQL, מפתחות camelCase בתוצאות. אין ORM.
- מיגרציות: קובצי SQL ב-`db/migrations`, רצות לפי סדר שם. אין לשנות מיגרציה
  שכבר נדחפה — רק להוסיף חדשה.
- שדות בבעלות המערכת (`rate`, `opening_balance`, `client_type`,
  `external_doc_client_id`, `notes`) לעולם לא נדרסים על ידי ייבוא האקסל.
- ת"ז/ח"פ מנורמלים ל-9 ספרות עם ריפוד אפסים (`normalizeTaxId`).

## בדיקות

- `npm run db:up` ואז `npm test` — בדיקות אינטגרציה מול Postgres אמיתי בדוקר
  (פורט 54330), כל קובץ בדיקות מתחיל מסכימה נקייה דרך `freshTestDb()`.
- כל שלב פיתוח מסתיים בבדיקות עוברות + lint + build לפני קומיט.
