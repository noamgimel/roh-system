# מערכת ניהול לקוחות, יתרות והנפקת מסמכים — משרד רו"ח

מערכת פנימית למשרד רואה חשבון: רשומת לקוחות, מעקב יתרות, קליטת דף חשבון בנק,
התאמת תנועות ללקוחות והנפקת מסמכים דרך ספק חיצוני (פייפרלס / Sumit).

האפיון הטכני המלא: `docs/tech-spec.md`.

## סטאק

- Next.js (App Router, TypeScript, Tailwind) — ממשק וצד שרת
- PostgreSQL — Supabase בפרודקשן, Docker מקומי בפיתוח ובבדיקות
- vitest — בדיקות אינטגרציה מול DB אמיתי

## הרצה מקומית

```bash
npm install
npm run db:up        # מרים Postgres מקומי (פיתוח + בדיקות) ב-Docker
npm run db:migrate   # מריץ מיגרציות על DB הפיתוח
npm run dev          # שרת פיתוח — http://localhost:3000
```

`.env.local` נדרש (ראה `.env.example`):

```
DATABASE_URL=postgres://roh:roh@localhost:54329/roh
CRON_SECRET=<סוד משותף למשימות מתוזמנות>
SESSION_SECRET=<לפחות 32 תווים אקראיים — openssl rand -hex 32>
```

## הזדהות

כל מסך וכל API חסומים בלי התחברות (proxy/middleware + עוגיית JWT חתומה,
HTTP-only). חריגים: `/login` ו-`/api/cron/*` (מוגן בסוד משותף משלו).
נעילה אוטומטית אחרי 5 ניסיונות כושלים ב-15 דקות; כל התחברות, כישלון
והתנתקות נרשמים ב-`audit_log` כולל IP.

יצירת המשתמש (פעם אחת):

```bash
ROH_USER_PASSWORD='<סיסמה חזקה>' npm run create-user -- <email> "<שם מלא>"
```

## חיוב חודשי (n8n)

משימה מתוזמנת ב-n8n ב-1 לכל חודש קוראת ל:

```
POST /api/cron/monthly-charges
Header: x-cron-secret: <CRON_SECRET>
Body (אופציונלי): { "period": "YYYY-MM" }
```

אידמפוטנטית — הרצה כפולה לא יוצרת חיוב כפול. אפשר להריץ גם ידנית
מכפתור במסך היתרות.

## בדיקות

```bash
npm run db:up   # אם עוד לא רץ
npm test
```

הבדיקות רצות מול DB ייעודי (פורט 54330, tmpfs) — כל ריצה מתחילה מסכימה נקייה.
כל הפיתוח והבדיקות על **נתונים ממוסכים בלבד** — אין נתוני אמת בריפו ובבדיקות.

`npm run fixtures` יוצר קובצי דוגמה ממוסכים לבדיקה ידנית של המסכים:
`fixtures/bank-sample.csv` (דף חשבון בקידוד windows-1255, כמו מהבנק) ו-
`fixtures/clients-sample.xlsx` (אקסל לקוחות).

## מבנה

```
db/migrations/        מיגרציות SQL — רצות לפי סדר שמות הקבצים
src/lib/              לוגיקה עסקית (db, migrate, audit, clients/)
src/app/              מסכים ו-API (App Router)
tests/                בדיקות אינטגרציה + עזרי בדיקה
scripts/migrate.ts    הרצת מיגרציות מה-CLI
```

## סטטוס שלבים

הפרויקט פוצל: **שלב א'** = כל המערכת בלי הנפקה (הזדהות, תור מלא, תאריך
חתך, יתרות פתיחה, יומן ביקורת, פרודקשן). **שלב ב'** = שכבת ההנפקה
(פייפרלס/Sumit דרך המתאם).

- [x] שלב 1 — סכימה, ייבוא/ייצוא אקסל, מודל הלקוחות
- [x] שלב 2 — פרסור CSV של דף החשבון, row_hash, מסך ביניים
- [x] שלב 3 — חילוץ המשלם וה-bank_key *(בנוי ונבדק על נתונים ממוסכים; ממתין לכיול על ייצוא אמיתי — `npm run reparse` אחרי כל שיפור בפרסר)*
- [x] שלב 4 — מנוע ההתאמה וטבלת הכינויים הלומדת
- [x] שלב 5 — מנוע היתרות והחיוב החודשי
- [x] שלב א' — הזדהות והגנת גישה
- [x] שלב א' — תור אישורים מלא (פיצול, חלקי) + תאריך חתך
- [x] שלב א' — מסך הזנת יתרות פתיחה + מסך יומן ביקורת
- [ ] שלב 6 — תור האישורים ויומן הביקורת (מסכים)
- [ ] שלב 7 — שכבת המתאם והחיבור לפייפרלס
